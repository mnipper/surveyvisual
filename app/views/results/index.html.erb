<%= render partial: 'navbar' %>

<% Question.each do |question| %>
  <% next if question.answers.empty? or question.screen_text.blank? %>
  <%= render partial: 'questions/question', object: question %>
<% end %>
