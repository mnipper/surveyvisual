<table class="table table-hover">
  <tr>
    <th>Participant</th>
    <th>Responses</th>
    <th>Last Response</th>
  </tr>
  <% @participants.each do |participant| %>
      <td><%= link_to participant._id, participant_path(participant) %></td>
      <td><%= participant.results.count %></td>
      <td><%= participant.results.last.last_result_time %></td>
    </tr>
  <% end %>
</table>
